/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
mutare_ecto-*.tar

# Dialyzer PLTs (rebuildable cache)
/priv/plts/*.plt
/priv/plts/*.plt.hash

# Mutare sandboxes the example READMEs prescribe (sibling dirs, so the ../.. path dep resolves)
examples/*_sandbox/
